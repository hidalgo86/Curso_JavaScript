/**
 * JavaScript no permite el uso de palabras reservadas para declarar variables,
 * a excepción de las que se encuentran en esta lista:
 * - implements
 * - interface
 * - let
 * - package
 * - private
 * - protected
 * - public
 * - static
 *
 * El uso de modo estricto evitará que esas palabras puedan ser utilizadas
 * como nombres de variables o funciones
 */

"use strict";

var let = 0;

let package = ["verano", "invierno", "primavera", "otoño"];

let interface = "Una cadena de caracteres";
