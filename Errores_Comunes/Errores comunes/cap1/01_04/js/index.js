/**
 * Los caracteres invisibles causan errores de sintaxis.
 * Corre este código en el navegador para que puedas ver el error:
 */

var miVariable = "un valor para esta variable";​

/**
 * No hay que confundir los caracteres de doble comillas.
 * Es recomendable usar un buen editor de código para detectar estos errores
 */

let unaVariableMas = ”Esto parece una cadena de caracteres”;